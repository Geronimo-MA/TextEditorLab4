<Window x:Class="TextEditorLab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TextEditorLab"
        Title="Безымянный — Текстовый редактор"
        Height="650" Width="1000"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Icon="Images/redo.png">

    <Window.Resources>
        <Style x:Key="ToolBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Width" Value="2"/>
            <Setter Property="Background" Value="LightGray"/>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Command="ApplicationCommands.New" Gesture="Ctrl+N"/>
        <KeyBinding Command="ApplicationCommands.Open" Gesture="Ctrl+O"/>
        <KeyBinding Command="ApplicationCommands.Save" Gesture="Ctrl+S"/>
        <KeyBinding Command="ApplicationCommands.Undo" Gesture="Ctrl+Z"/>
        <KeyBinding Command="ApplicationCommands.Redo" Gesture="Ctrl+Y"/>
        <KeyBinding Command="ApplicationCommands.Cut" Gesture="Ctrl+X"/>
        <KeyBinding Command="ApplicationCommands.Copy" Gesture="Ctrl+C"/>
        <KeyBinding Command="ApplicationCommands.Paste" Gesture="Ctrl+V"/>
        <KeyBinding Command="ApplicationCommands.SelectAll" Gesture="Ctrl+A"/>
    </Window.InputBindings>

    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.New" Executed="NewFile_Click"/>
        <CommandBinding Command="ApplicationCommands.Open" Executed="OpenFile_Click"/>
        <CommandBinding Command="ApplicationCommands.Save" Executed="SaveFile_Click"/>

        <CommandBinding Command="ApplicationCommands.Undo" Executed="Undo_Click"/>
        <CommandBinding Command="ApplicationCommands.Redo" Executed="Redo_Click"/>
        <CommandBinding Command="ApplicationCommands.Cut" Executed="Cut_Click"/>
        <CommandBinding Command="ApplicationCommands.Copy" Executed="Copy_Click"/>
        <CommandBinding Command="ApplicationCommands.Paste" Executed="Paste_Click"/>
        <CommandBinding Command="ApplicationCommands.SelectAll" Executed="SelectAll_Click"/>
    </Window.CommandBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="WhiteSmoke" Height="30">
            <MenuItem Header="_Файл">
                <MenuItem Header="_Создать" Command="ApplicationCommands.New" InputGestureText="Ctrl+N">
                    <MenuItem.Icon>
                        <Image Source="Images/new.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="_Открыть" Command="ApplicationCommands.Open" InputGestureText="Ctrl+O">
                    <MenuItem.Icon>
                        <Image Source="Images/open.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="_Сохранить" Command="ApplicationCommands.Save" InputGestureText="Ctrl+S">
                    <MenuItem.Icon>
                        <Image Source="Images/save.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="Сохранить _как..." Click="SaveAsFile_Click">
                    <MenuItem.Icon>
                        <Image Source="Images/save_as.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <Separator/>
                <MenuItem Header="_Выход" Click="Exit_Click" InputGestureText="Alt+F4"/>
            </MenuItem>

            <MenuItem Header="_Правка">
                <MenuItem Header="_Отменить" Command="ApplicationCommands.Undo" InputGestureText="Ctrl+Z">
                    <MenuItem.Icon>
                        <Image Source="Images/undo.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="_Повторить" Command="ApplicationCommands.Redo" InputGestureText="Ctrl+Y">
                    <MenuItem.Icon>
                        <Image Source="Images/redo.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <Separator/>

                <MenuItem Header="_Вырезать" Command="ApplicationCommands.Cut" InputGestureText="Ctrl+X">
                    <MenuItem.Icon>
                        <Image Source="Images/cut.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="_Копировать" Command="ApplicationCommands.Copy" InputGestureText="Ctrl+C">
                    <MenuItem.Icon>
                        <Image Source="Images/copy.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="_Вставить" Command="ApplicationCommands.Paste" InputGestureText="Ctrl+V">
                    <MenuItem.Icon>
                        <Image Source="Images/paste.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="_Удалить" Click="Delete_Click" InputGestureText="Del"/>
                <Separator/>
                <MenuItem Header="_Выделить всё" Command="ApplicationCommands.SelectAll" InputGestureText="Ctrl+A"/>
            </MenuItem>

            <MenuItem Header="_Текст">
                <MenuItem Header="Постановка задачи" Click="ShowTextInfo_Click"/>
                <MenuItem Header="Грамматика" Click="ShowTextInfo_Click"/>
                <MenuItem Header="Классификация грамматики" Click="ShowTextInfo_Click"/>
                <MenuItem Header="Метод анализа" Click="ShowTextInfo_Click"/>
                <MenuItem Header="Тестовый пример" Click="ShowTextInfo_Click"/>
                <MenuItem Header="Список литературы" Click="ShowTextInfo_Click"/>
                <MenuItem Header="Исходный код программы" Click="ShowTextInfo_Click"/>
            </MenuItem>

            <MenuItem Header="_Пуск" Click="RunAnalyzer_Click"/>

            <MenuItem Header="_Справка">
                <MenuItem Header="Вызов справки" Click="Help_Click" InputGestureText="F1">
                    <MenuItem.Icon>
                        <Image Source="Images/help.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="О программе" Click="About_Click">
                    <MenuItem.Icon>
                        <Image Source="Images/info.png" Width="16" Height="16"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        <ToolBar Grid.Row="1" Background="WhiteSmoke" Height="40">
            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Создать (Ctrl+N)" Command="ApplicationCommands.New">
                <Image Source="Images/new.png" Width="20" Height="20"/>
            </Button>
            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Открыть (Ctrl+O)" Command="ApplicationCommands.Open">
                <Image Source="Images/open.png" Width="20" Height="20"/>
            </Button>
            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Сохранить (Ctrl+S)" Command="ApplicationCommands.Save">
                <Image Source="Images/save.png" Width="20" Height="20"/>
            </Button>

            <Separator/>

            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Отменить (Ctrl+Z)" Command="ApplicationCommands.Undo">
                <Image Source="Images/undo.png" Width="20" Height="20"/>
            </Button>
            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Повторить (Ctrl+Y)" Command="ApplicationCommands.Redo">
                <Image Source="Images/redo.png" Width="20" Height="20"/>
            </Button>

            <Separator/>

            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Вырезать (Ctrl+X)" Command="ApplicationCommands.Cut">
                <Image Source="Images/cut.png" Width="20" Height="20"/>
            </Button>
            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Копировать (Ctrl+C)" Command="ApplicationCommands.Copy">
                <Image Source="Images/copy.png" Width="20" Height="20"/>
            </Button>
            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Вставить (Ctrl+V)" Command="ApplicationCommands.Paste">
                <Image Source="Images/paste.png" Width="20" Height="20"/>
            </Button>

            <Separator/>

            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Запуск анализатора" Click="RunAnalyzer_Click">
                <Image Source="Images/run.png" Width="20" Height="20"/>
            </Button>

            <Separator/>

            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="Справка (F1)" Click="Help_Click">
                <Image Source="Images/help.png" Width="20" Height="20"/>
            </Button>
            <Button Style="{StaticResource ToolBarButtonStyle}" ToolTip="О программе" Click="About_Click">
                <Image Source="Images/info.png" Width="20" Height="20"/>
            </Button>
        </ToolBar>

        <TextBox x:Name="EditorTextBox"
                 Grid.Row="2"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="14"
                 BorderThickness="1"
                 BorderBrush="Gray"
                 Padding="5"
                 TextChanged="EditorTextBox_TextChanged"
                 SelectionChanged="EditorTextBox_SelectionChanged" />

        <GridSplitter Grid.Row="3"
                      Height="5"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      Background="LightGray"
                      ShowsPreview="True"
                      ResizeDirection="Rows" />

		<Grid Grid.Row="4" Margin="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<StackPanel Grid.Row="0"
						Orientation="Horizontal"
						Margin="5">
				<TextBlock Text="Тип поиска:"
						   VerticalAlignment="Center"
						   Margin="0,0,8,0"/>

				<ComboBox x:Name="SearchTypeComboBox"
						  Width="260"
						  SelectedIndex="0"
						  Margin="0,0,10,0">
					<ComboBoxItem Content="Закрывающие HTML-теги"/>
					<ComboBoxItem Content="Имя пользователя"/>
					<ComboBoxItem Content="Широта"/>
				</ComboBox>

				<Button x:Name="RunRegexButton"
						Content="Найти"
						Width="100"
						Height="28"
						Click="RunAnalyzer_Click"
						Margin="0,0,12,0"/>

				<TextBlock x:Name="MatchesCountText"
						   Text="Найдено: 0"
						   VerticalAlignment="Center"
						   FontWeight="SemiBold"/>
			</StackPanel>

			<DataGrid x:Name="ResultsDataGrid"
					  Grid.Row="1"
					  Margin="5"
					  AutoGenerateColumns="False"
					  IsReadOnly="True"
					  SelectionMode="Single"
					  SelectionUnit="FullRow"
					  CanUserAddRows="False"
					  CanUserDeleteRows="False"
					  HeadersVisibility="Column"
					  SelectionChanged="ResultsDataGrid_SelectionChanged">
				<DataGrid.Columns>
					<DataGridTextColumn Header="Найденная подстрока"
										Binding="{Binding MatchText}"
										Width="*"/>
					<DataGridTextColumn Header="Строка"
										Binding="{Binding Line}"
										Width="90"/>
					<DataGridTextColumn Header="Символ"
										Binding="{Binding Column}"
										Width="90"/>
					<DataGridTextColumn Header="Длина"
										Binding="{Binding Length}"
										Width="90"/>
				</DataGrid.Columns>
			</DataGrid>
		</Grid>

        <StatusBar Grid.Row="5" Background="LightGray" Height="25">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Готов"/>
            </StatusBarItem>

            <Separator/>

            <StatusBarItem>
                <TextBlock x:Name="CursorPositionText" Text="Стр: 1  Стб: 1"/>
            </StatusBarItem>

            <StatusBarItem>
                <TextBlock x:Name="FileSizeText" Text="Размер: 0 байт"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>